package com.example.aiassist.config;

// CorsConfig is intentionally disabled.
// CORS is now fully handled by SecurityConfig.corsConfigurationSource()
// to avoid duplicate bean conflicts with Spring Security's CORS chain.
